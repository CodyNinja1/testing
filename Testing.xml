<manialink version="3">
<script>
<!-- 
    main() {
        log("Init MoonGravity Manialink");
        while (True) {
            yield;
            if (GUIPlayer != Null && GUIPlayer == InputPlayer) {
                if (GUIPlayer.AccelCoef != 2.3) {
                    log("Changing the Gravity for the player " ^ GUIPlayer.User.Name);
                    GUIPlayer.AccelCoef = 2.3;
                }
                if (GUIPlayer.ControlCoef != 2.0) {
                    log("Changing the Gravity for the player " ^ GUIPlayer.User.Name);
                    GUIPlayer.ControlCoef = 2.0;
                }
                if (GUIPlayer.AdherenceCoef != 2.0) {
                    log("Changing the Gravity for the player " ^ GUIPlayer.User.Name);
                    GUIPlayer.AdherenceCoef = 2.0;
                }
                if (GUIPlayer.GravityCoef != 10.0) {
                    log("Changing the Gravity for the player " ^ GUIPlayer.User.Name);
                    GUIPlayer.GravityCoef = 10.0;
                }
            }
        }
    }
     -->
</script>
</manialink>
